#pragma once

/*
#include <stdint.h>

#include "sysclock.h"
#include "leds.h"
#include "buttons.h"


class Ui
{
    public:
        Ui(SysClock& clock) : m_clock(clock) {};
        void init(); // pinmodes ...
        void update();
        void testmode();

    private:
        SysClock& m_clock;
        Leds m_lights = Leds(m_clock);
        Buttons m_buttons;


        uint16_t m_capture_blink_speed = 500; //ms
        uint16_t m_confirmation_blink_speed = 100; //ms
};
*/
