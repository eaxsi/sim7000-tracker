#pragma once

/*
#include "button.h"
#include "hardware_configuration.h"


class Buttons
{
    public:
        Buttons() {};
        void update();
        uint8_t which_button_pressed();
        bool all_pressed();
    private:
        Button m_buttons[2] = { Button(REED_PIN), Button(ACC_SENSOR_PIN) };
        bool m_button_lock[1] = { false };

};
*/
