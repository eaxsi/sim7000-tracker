#pragma once

#include <stdint.h>
#include "Arduino.h"

/*
class Led
{
    public:
        Led(uint8_t pin, bool reverse_logic) : m_pin(pin), m_reverse_logic(reverse_logic){pinMode(m_pin, OUTPUT);};
        void set(bool);
        bool get();
        void turn_on(void);
        void turn_off(void);
    private:
        uint8_t m_pin;
        bool m_state;
        bool m_reverse_logic;
};
*/
