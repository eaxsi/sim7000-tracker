#include "ui.h"

/*
void Ui::update()
{
    m_lights.update();
    m_buttons.update();
}

void Ui::testmode()
{
    //common::party button = get_requested_party();  
    
    if(m_buttons.which_button_pressed())
    {
        Serial.println("Button pressed!");
        m_lights.static_light();
        update();
        delay(100);
    }
    else
        m_lights.turn_off();


    update();
    delay(100);
}
*/


